# Electron comes from the patched mirror, not from upstream. The second line is
# required: the electron package verifies downloads against a bundled checksum
# list by default and would reject the mirror's bytes.
electron_mirror=https://github.com/csarkosh/electron-gamepatch/releases/download/
electron_use_remote_checksums=1
save-exact=true
