/**
 * The gust ω constants (rad/s) that `windPlugin.ts`'s GLSL inlines, exported
 * so `motesParams.ts` rides the same wave. Babylon-free, unlike
 * `windPlugin.ts`, which is a Babylon material plugin.
 */
export const WIND_OMEGA_GUST = 0.3769911184;
export const WIND_OMEGA_GUST2 = 0.879645943;
